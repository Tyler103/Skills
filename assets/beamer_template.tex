% ============================================================
% Beamer template produced by pptx-to-beamer skill
% Compile with: pdflatex presentation.tex
%               (run twice for correct cross-references)
% ============================================================

\documentclass{beamer}

% ── Theme ────────────────────────────────────────────────────
\usetheme{Madrid}          % Change to any theme from references/beamer-themes.md
% \usecolortheme{dolphin}  % Optional: overlay a color theme

% ── Packages ─────────────────────────────────────────────────
\usepackage{graphicx}      % \includegraphics
\usepackage{hyperref}      % clickable links
\usepackage{amsmath}       % math environments
\usepackage{booktabs}      % \toprule / \midrule / \bottomrule in tables
\usepackage[utf8]{inputenc}% UTF-8 source encoding

% ── Metadata ─────────────────────────────────────────────────
\title{Presentation Title}
\subtitle{Optional Subtitle}
\author{Author Name}
\institute{Institution}
\date{\today}

% ============================================================
\begin{document}
% ============================================================

% Title slide
\begin{frame}
  \titlepage
\end{frame}

% Table of contents (optional — comment out if not needed)
\begin{frame}{Outline}
  \tableofcontents
\end{frame}

% ── Example frames ───────────────────────────────────────────

\begin{frame}{Slide with Bullets}
  \begin{itemize}
    \item First top-level point
    \begin{itemize}
      \item Nested sub-point
      \item Another sub-point
    \end{itemize}
    \item Second point with \textbf{bold} and \textit{italic} text
    \item Third point with \underline{underlined} text
  \end{itemize}
\end{frame}

\begin{frame}{Slide with a Table}
  \begin{center}
    \begin{tabular}{|l|c|r|}
      \hline
      \textbf{Column A} & \textbf{Column B} & \textbf{Column C} \\
      \hline
      Row 1 value       & 42                & 3.14              \\
      Row 2 value       & 99                & 2.71              \\
      \hline
    \end{tabular}
  \end{center}
\end{frame}

\begin{frame}{Slide with an Image}
  \begin{center}
    \includegraphics[width=0.7\textwidth]{beamer_images/example_image.png}
  \end{center}
\end{frame}

\begin{frame}{Slide with Two Columns}
  \begin{columns}
    \column{0.5\textwidth}
      Left column content here.
      \begin{itemize}
        \item Point A
        \item Point B
      \end{itemize}
    \column{0.5\textwidth}
      \includegraphics[width=\textwidth]{beamer_images/example_image.png}
  \end{columns}
\end{frame}

% ============================================================
\end{document}
% ============================================================
